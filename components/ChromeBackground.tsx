/**
 * B4 "Marble Luxe" background — cool stone with faint warm veining.
 * (Filename kept from the earlier chrome design so imports stay stable.)
 */
export default function ChromeBackground() {
  return <div className="marble-bg" />;
}
